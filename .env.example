# AWS Configuration
# AWS region where S3 and Textract services are located (e.g., us-east-1, us-west-2)
AWS_REGION=us-east-1

# AWS Access Key ID for authentication
# SECURITY WARNING: Never commit this file to version control. Use AWS IAM roles in production.
AWS_ACCESS_KEY_ID=your_access_key_id_here

# AWS Secret Access Key for authentication
# SECURITY WARNING: Keep this secret and never expose it in client-side code.
AWS_SECRET_ACCESS_KEY=your_secret_access_key_here

# S3 Bucket name where PDF files will be stored
AWS_S3_BUCKET_NAME=your-pdf-bucket-name

# Application URL for generating presigned URLs and links
# Used in development: http://localhost:3000
# Used in production: https://yourdomain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# MongoDB Configuration
# MongoDB connection string for storing PDF metadata
# Format: mongodb://username:password@host:port/database
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/database
# For local MongoDB: mongodb://localhost:27017/pdf_extractor
MONGODB_URI=mongodb://localhost:27017/pdf_extractor

# MongoDB database name (optional, defaults to 'pdf_extractor')
MONGODB_DATABASE=pdf_extractor

# Required IAM Permissions for AWS credentials:
# - s3:PutObject - Allow uploading PDFs to S3
# - s3:GetObject - Allow retrieving PDFs from S3
# - textract:DetectDocumentText - For synchronous text detection
# - textract:StartDocumentTextDetection - Start async text extraction job
# - textract:GetDocumentTextDetection - Poll extraction job status

